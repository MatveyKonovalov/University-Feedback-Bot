# University Feedback Bot 🤖

Бот для сбора и просмотра отзывов об учебных заведениях. Позволяет пользователям анонимно оставлять отзывы о своих вузах и читать отзывы других пользователей.

## 🚀 Функционал

- **📝 Оставить отзыв**: Анонимное добавление отзыва о вузе с указанием:
  - Название университета
  - Подразделение/факультет
  - Текст отзыва
  - Оценка по 10-балльной шкале
- **📖 Чтение отзывов**: Поиск и просмотр отзывов по конкретным вузам
- **📊 Статистика**: Средняя оценка вуза, список подразделений с отзывами
- **🏠 Навигация**: Удобное меню с кнопками для навигации

## 🛠 Технологии

- **Python 3.11+**
- **Aiogram 3.x** - асинхронный фреймворк для Telegram Bot API
- **SQLite3** - база данных для хранения отзывов
- **FSM (Finite State Machine)** - управление состояниями диалога

## 📁 Структура проекта

```
Aiogramproject/
├── .venv/                    # Виртуальное окружение
├── app/
│   ├── handlers.py          # Основной файл с обработчиками
│   ├── keyboard.py          # Клавиатуры и кнопки
│   └── statecom.py          # Состояния FSM
├── image/                   # Изображения для бота
│   ├── img.png
│   └── img_start.png
├── .env                     # Переменные окружения (токен бота)
├── database_comment.db      # База данных SQLite
└── main.py                 # Точка входа
```

## ⚙️ Установка и запуск

### 1. Клонирование и настройка
```bash
# Клонировать репозиторий
git clone <repository-url>
cd Aiogramproject

# Создать виртуальное окружение
python -m venv .venv

# Активировать виртуальное окружение
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate
```

### 2. Установка зависимостей
```bash
pip install aiogram python-dotenv
```

### 3. Настройка бота
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=ваш_токен_бота_здесь
```

### 4. Запуск бота
```bash
python main.py
```

## 🗄️ База данных

Бот использует SQLite базу данных `database_comment.db` со следующей структурой:

### Основные таблицы:
1. `university` - список всех вузов
   - `id` (INTEGER PRIMARY KEY)
   - `name` (TEXT) - название вуза

2. Таблицы для каждого вуза (создаются динамически)
   - `sub` (TEXT) - подразделение/факультет
   - `comment` (TEXT) - текст отзыва
   - `grade` (INTEGER) - оценка (1-10)

## 📋 Команды бота

- `/start` - Начало работы с ботом
- Кнопка "Оставить отзыв" - Процесс добавления нового отзыва
- Кнопка "Читать отзывы" - Поиск и просмотр существующих отзывов

## 🔄 Процесс работы

### Оставить отзыв:
1. Выбор "Оставить отзыв"
2. Ввод названия вуза
3. Ввод подразделения
4. Написание текста отзыва
5. Указание оценки (1-10)
6. Подтверждение корректности данных

### Читать отзывы:
1. Выбор "Читать отзывы"
2. Ввод названия вуза
3. Просмотр статистики вуза
4. Выбор подразделения
5. Просмотр всех отзывов по выбранному подразделению

## 🛡️ Особенности

- **Анонимность**: Все отзывы сохраняются анонимно
- **Валидация**: Проверка корректности вводимых данных
- **Подтверждение**: Подтверждение правильности данных перед сохранением
- **Обработка ошибок**: Корректная обработка неверных вводов
- **Навигация**: Удобные кнопки для возврата в главное меню

## 📸 Интерфейс

Бот использует:
- Интерактивные кнопки для навигации
- Изображения для визуального оформления
- Форматированные сообщения со статистикой
- Пошаговый ввод данных через FSM

## 🔧 Возможные улучшения

- Добавление фильтрации отзывов по дате
- Реализация системы рейтинга отзывов
- Добавление возможности редактирования отзывов
- Экспорт данных в Excel/PDF
- Админ-панель для модерации отзывов
- Поддержка нескольких языков
- Интеграция с внешними API для проверки вузов

## ⚠️ Важно

- Все данные сохраняются локально в SQLite базе
- Рекомендуется регулярно создавать резервные копии базы данных
- Для продакшена рекомендуется использовать PostgreSQL
- Не забудьте добавить `.env` в `.gitignore`

## 👥 Для разработчиков

### Основные файлы:
- `app/handlers.py` - Основная логика бота
- `app/keyboard.py` - Клавиатуры и кнопки
- `app/statecom.py` - Состояния FSM
- `main.py` - Точка входа и запуск бота

### Создание нового обработчика:
1. Добавьте декоратор `@router.callback_query()` или `@router.message()`
2. Реализуйте функцию-обработчик
3. При необходимости используйте FSM для многошаговых диалогов

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

### Автор
Разработано для помощи абитуриентам в выборе учебного заведения на основе реальных отзывов студентов.

### Поддержка
Для вопросов и предложений создавайте issue в репозитории проекта.
